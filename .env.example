# Database - Add connection pooling for better performance
# Use pgBouncer or connection pooling mode for production
DATABASE_URL="postgresql://username:password@localhost:5432/gms_report?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Set for production
# NEXTAUTH_SECRET can be generated with: openssl rand -base64 32
